name: lhenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - -e .   # gethypercube editable (pulls numpy, scipy)
    - matplotlib
    - pandas
    - jupyter
    - ipykernel
    - pytest
